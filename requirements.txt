# ── Core data & visualisation ─────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.13.0
Pillow>=10.0.0
scipy>=1.10.0                # used in various notebooks for stats / interpolation
scikit-learn>=1.3.0          # label encoding, metrics utilities

# ── Jupyter ───────────────────────────────────────────────────────────────────
jupyterlab>=4.0.0
ipython>=8.0.0
ipywidgets>=8.0.0
ipympl>=0.9.0                # interactive matplotlib in JupyterLab
tqdm>=4.65.0

# ── Video / media ─────────────────────────────────────────────────────────────
opencv-python>=4.8.0
moviepy>=1.0.3               # video_processing_pipeline.ipynb
imageio-ffmpeg>=0.4.0        # ffmpeg backend for imageio / moviepy
requests>=2.31.0             # downloading assets / Label Studio API calls

# ── PyTorch (CPU/GPU/MPS — install the right variant for your platform) ───────
# See https://pytorch.org/get-started/locally/ for CUDA/MPS-specific wheels.
# The pip default below installs CPU-only; override if you have a GPU.
torch>=2.0.0
torchvision>=0.15.0

# ── Detection & tracking ──────────────────────────────────────────────────────
ultralytics>=8.3.0           # YOLOv8, YOLO11, RT-DETR, ByteTrack, BoTSORT
boxmot>=10.0.0               # 05_track_rtdetr.ipynb — additional tracker backends

# ── Label Studio integration ──────────────────────────────────────────────────
label-studio-sdk>=0.0.30     # labelstudio_to_yolo.ipynb, ml_backend

# ── Config / serialisation ────────────────────────────────────────────────────
pyyaml>=6.0
